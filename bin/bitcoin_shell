#!/usr/bin/env ruby
$:.unshift( File.expand_path("../../lib", __FILE__) )
require 'bitcoin_old'

include BitcoinOld
include BitcoinOld::Util

BitcoinOld.network = ARGV.shift || :bitcoin

require 'irb'
require 'irb/completion'
IRB.start
